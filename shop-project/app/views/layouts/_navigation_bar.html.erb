<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
<div class="">
     <a href="/" class="item">

     </a>
     <div class="right menu">
       <% if logged_in? %>

         <%= link_to 'Logout', logout_path, {class: 'item'} %>
       <% else %>
       <%= link_to 'Login', login_path, {class: 'item'} %>
     <% end %>
</div>
  <a class="navbar-brand" href="#">Welcome Adventurer!</a>

  <div class="collapse navbar-collapse" id="navbarNav">
    <% if session[:user_id] %>

      <% if session[:isTraveler]%>
      <!-- Travelers buttons-->
      <ul id="the-list" class="navbar-nav">
        

        <li class="nav-item">
          <%= button_to "Edit", traveler_edit_path, method: :get, class: 'btn btn-info'%>
        </li>

        <li class="nav-item">
          <%= button_to "Shop", traveler_shopping_path, method: :get, class: 'btn btn-info'%>
        </li>

        <li class="nav-item">
          <%= button_to "Home", traveler_path, method: :get, class: 'btn btn-info'%>
        </li>

      </ul>

    
      <% else %>
        <!-- Shopkeepers buttons-->
        <ul id="the-list" class="navbar-nav">

          <li class="nav-item">
            <%= button_to "Edit", shopkeeper_editstore_path, method: :get, class: 'btn btn-info'%>
          </li>

          <li class="nav-item">
            <%= button_to "Dashboard", shopkeeper_analytics_path, method: :get, class: 'btn btn-info'%>
          </li>

          <li class="nav-item">
            <%= button_to "Home", shopkeeper_path, method: :get, class: 'btn btn-info'%>
          </li>

        </ul>
      <% end %>
    <% end %>
  </div>
</nav>

